#include <stdio.h>

int main() {
    printf("This is my program!\n");
    return 0;
}

// Comado para compilar -> gcc program.c, crea un archivo ejecutable con el nombre de a.exe
//                         gcc program.c -o test, crea un archivo ejecutable con el nombre de text.exe